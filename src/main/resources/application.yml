spring:
  application:
    name: review-service

server:
  port: 7002

logging:
  level:
    root: INFO
    com.microservices: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker

server:
  port: 8080